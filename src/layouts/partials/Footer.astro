---
import Logo from "@/components/Logo.astro";
import config from "@/config/config.json";
import menu from "@/config/menu.json";
import social from "@/config/social.json";
import DynamicIcon from "@/helpers/DynamicIcon";
import { markdownify } from "@/lib/utils/textConverter";
const { copyright } = config.params;

export interface ISocial {
  name: string;
  icon: string;
  link: string;
}
---

<footer class="bg-light dark:bg-darkmode-light">
  <div class="container">
    <div
      class="grid grid-cols-1 md:grid-cols-4 gap-8 py-10 py-10 md:pt-20 md:pb-14"
    >


      <!-- <ul class="flex gap-x-4 lg:gap-x-10 my-3">
        {
          menu.footer.map((menu) => (
            <li class="footer-link">
              <a href={menu.url}>{menu.name}</a>
            </li>
          ))
        }
      </ul> -->
 <div style="text-align: center;">
        <Logo/>
        <p >
          Welcome to Pyromo CottonBox Pvt Ltd, leading manufacturers of superior corrugated boxes since 2019. We offer the products in all shapes and sizes.
        </p>
      </div>

      <!-- {/* social share */}
      <ul class="social-icons social-icons-footer">
        {
          social?.main.map((social: ISocial) => (
            <li>
              <a
                aria-label={social.name}
                href={social.link}
                target="_blank"
                rel="noopener noreferrer nofollow"
              >
                <span class="sr-only">{social.name}</span>
                <DynamicIcon className="inline-block" icon={social.icon} />
              </a>
            </li>
          ))
        }
      </ul> -->


      <!-- Column 2: Menu -->
      <div style="text-align: center;">
        <h4 class="font-bold mb-3">MENU</h4>
        <ul class="space-y-1">
          <li><a href="/">HOME</a></li>
          <li><a href="/about">ABOUT US</a></li>
          <li><a href="/products">PRODUCTS</a></li>
          <li><a href="/contact">CONTACT US</a></li>
        </ul>
      </div>

      <!-- Column 3: Quick Links -->
      <div style="text-align: center;">
        <h4 class="font-bold mb-3">QUICK LINKS</h4>
        <ul class="space-y-1">
          <li><a href="/privacy-policy">PRIVACY POLICY</a></li>
          <li><a href="/terms-and-conditions">TERMS AND CONDITIONS</a></li>
        </ul>
      </div>

      <!-- Column 4: Contact Info -->
      <div style="text-align: center;">
        <h4 class="font-bold mb-3">GET IN TOUCH</h4>
        <p>Address: No. 45/20, Mookathal Street, Perambur Purasawalkam, Vepery, Chennai 600007</p>
        <p>Contact: +91 8825403543</p>
        <p>Email: <a href="mailto:info@pyromocottonbox.in">info@pyromocottonbox.in</a></p>
      </div>
    </div>
    </div>

    <div class="border-t border-border py-5 dark:border-darkmode-border">
      <div
        class="flex flex-col md:flex-row gap-y-2 justify-between items-center text-text-light dark:text-darkmode-text-light"
      >
        <ul class="flex gap-x-4">
          {
            menu.footerCopyright.map((menu) => (
              <li class="footer-link">
                <a href={menu.url}>{menu.name}</a>
              </li>
            ))
          }
        </ul>

        <p class="text-sm font-light" set:html={markdownify(copyright)} />
      </div>
    </div>
  </div>
</footer>
